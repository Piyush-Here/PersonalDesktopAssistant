# ── Core server ───────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
jinja2>=3.1.4

# ── Document reading ──────────────────────────────────────────────────────────
python-docx>=1.1.0
openpyxl>=3.1.2
python-pptx>=0.6.23

# ── PDF reading (PyMuPDF preferred — faster and more reliable than pypdf) ─────
pymupdf>=1.24.0

# ── Desktop automation ────────────────────────────────────────────────────────
pyautogui>=0.9.54          # mouse, keyboard, hotkeys
pillow>=10.3.0             # screenshot capture (also required by pyautogui)

# ── Windows UI Automation (optional — needed for click "Save" / type into field)
pywinauto>=0.6.8

# ── OCR (optional — also requires Tesseract binary on PATH) ──────────────────
pytesseract>=0.3.10

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=8.2.0
