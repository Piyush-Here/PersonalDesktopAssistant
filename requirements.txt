# Core
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
jinja2>=3.1.4

# Document reading
python-docx>=1.1.0
openpyxl>=3.1.2
python-pptx>=0.6.23

# Optional — install for screen observation
pillow>=10.3.0           # screenshot capture
pytesseract>=0.3.10      # OCR (also needs Tesseract binary on PATH)
pywinauto>=0.6.8         # Windows UI Automation

# Optional — install for PDF reading
pymupdf>=1.24.0          # PyMuPDF (fitz)

# Dev / testing
pytest>=8.2.0
